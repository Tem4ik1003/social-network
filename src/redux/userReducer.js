const initialState = {
    userName: '',
    friends: [],
    favoritePages: [],
    favoriteGroups: []
}

export const userReducer  = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}